# Assignment 2: Sentiment Classification

Movie review sentiment classifier trained on train.csv, evaluated on public_test.csv.
Final model: TF-IDF + VADER features -> Logistic Regression.

## How to run
1. pip install -r requirements.txt
2. Open and run stage1_notebook.ipynb top to bottom.